import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bgTop    = Color(0xFF1A0533);
  static const bgBottom = Color(0xFF0A1A4A);

  // Car body options (bright, child-friendly)
  static const carColors = [
    Color(0xFFFF3B3B), // red
    Color(0xFF00AAFF), // sky blue
    Color(0xFF00CC55), // green
    Color(0xFFFF9900), // orange
    Color(0xFFBB44FF), // purple
    Color(0xFFFF44AA), // pink
    Color(0xFFFFDD00), // yellow
    Color(0xFF00DDCC), // teal
  ];

  // UI
  static const starYellow = Color(0xFFFFE566);
  static const white      = Color(0xFFFFFFFF);
  static const grey       = Color(0xFF8888AA);

  // Scratch cover (gift-wrap silver)
  static const scratchBase   = Color(0xFFB8B8CC);
  static const scratchDark   = Color(0xFF8888AA);
  static const ribbonRed     = Color(0xFFFF3366);
  static const ribbonGold    = Color(0xFFFFCC00);

  // Sparkle palette
  static const sparkles = [
    Color(0xFFFFDD00),
    Color(0xFFFF66FF),
    Color(0xFF00FFCC),
    Color(0xFFFF6600),
    Color(0xFF00AAFF),
    Color(0xFFFFFFFF),
  ];

  // Confetti palette
  static const confetti = [
    Color(0xFFFF3B3B),
    Color(0xFFFF9900),
    Color(0xFFFFDD00),
    Color(0xFF00CC55),
    Color(0xFF00AAFF),
    Color(0xFFBB44FF),
    Color(0xFFFF44AA),
    Color(0xFFFFFFFF),
  ];

  // Play buttons
  static const btnHorn  = Color(0xFFFF9900);
  static const btnLight = Color(0xFFFFDD00);
  static const btnWash  = Color(0xFF00AAFF);
  static const btnParty = Color(0xFFFF44AA);
  static const btnSpin  = Color(0xFF00CC55);
  static const btnColor = Color(0xFFBB44FF);
  static const btnAgain = Color(0xFFFF3B3B);
}
